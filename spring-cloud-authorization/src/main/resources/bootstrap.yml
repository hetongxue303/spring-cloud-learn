spring:
  application:
    name: spring-cloud-authorization
  cloud:
    nacos:
      username: nacos
      password: nacos
      server-addr: 127.0.0.1:8848
      discovery:
        namespace: 41feead3-12a9-458e-9633-04e1097cba8f
        group: spring-cloud
        server-addr: ${spring.cloud.nacos.server-addr}
      config:
        namespace: ${spring.cloud.nacos.discovery.namespace}
        group: ${spring.cloud.nacos.discovery.group}
        server-addr: ${spring.cloud.nacos.server-addr}
        file-extension: yml
        refresh-enabled: true
  profiles:
    active: dev